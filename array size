#include <stdio.h>


int main()
{
	int i;
	int arr[] = { 0, 0, 0, 0 };
	size_t n = sizeof(arr) / sizeof(arr[0]);
	printf("The size of the array is: %ld", n);
	printf("\nThe elements are:\n");
	for (i = 0; i < n; i++)
	printf(" %d ", arr[i]);
	getchar();
	return 0;
}

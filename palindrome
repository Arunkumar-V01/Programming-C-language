#include <stdio.h>
int main()
{
    int a,b,rev,k;
    rev=0;
    scanf("%d",&a);
    k=a;
    while(a>0)
    
        b=a%10;
        rev=rev*10+b;
        a=a/10;
    }
    printf("%d",rev);
if(k==rev)
printf("palindrome");
else
printf("not a palindrome");
}

